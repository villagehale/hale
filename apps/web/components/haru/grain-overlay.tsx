/**
 * Intentionally a no-op for the Almanac design system. Paper texture is
 * implied by the bone + vellum palette rather than a noise overlay.
 * Kept for backwards compatibility with any callers that still import it.
 */
export function GrainOverlay() {
  return null;
}
